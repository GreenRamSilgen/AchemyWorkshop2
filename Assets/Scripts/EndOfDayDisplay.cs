using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class EndOfDayDisplay : MonoBehaviour
{
    public Text ledger;
    public Text day;

    // Use this for initialization
    void Start()
    {
        ledger.text = "    ledger:";
      
    }

    // Update is called once per frame
    void Update()
    {
    }

    int calcHireCost()
    {
        //foreach (object kv in Global.gatherers)
        //{

        //}
        
        return 0;
    }
}
